type Access {
  command: String
  groupId: Int
}

extend type Query {
  access: [Access]
}

extend type Mutation {
  addAccess(groupId: Int!, command: String): Access
  deleteAccess(groupId: Int!): Boolean!
}
