type Environment {
  id: Int
  name: String
  attributes: [ReverseAttr]
}

extend type Query {
  environments: [Environment]
}

extend type Mutation {
  addEnvironment(name: String!): Environment
  updateEnvironment(environmentId: Int!, name: String): Environment!
  deleteEnvironment(environmentId: Int!): Boolean!
}
