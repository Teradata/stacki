type Environment {
  id: Int
  name: String
  attributes: [ReverseAttr]
}

extend type Query {
  environments: [Environment]
}

extend type Mutation {
  addEnvironment(name: String!,): Environment
  updateEnvironment(id: Int!, name: String): Environment!
  deleteEnvironment(id: Int!): Boolean!
}