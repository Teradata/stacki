type ScopeMap {
  id: Int
  scope: String
  applianceId: Int
  appliance: Appliance
  osId: Int
  os: Os
  environmentId: Int
  nodeId: Int
}

extend type Query {
  scopeMaps: [ScopeMap]
  scopeMap(id: Int, scope: String): ScopeMap
}
