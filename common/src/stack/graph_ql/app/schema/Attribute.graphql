type Attribute {
	id: Int
	scope_map_id: Int
	scope_map: ScopeMap
	name: String
	value: String
}

extend type Query {
	attributes(id: Int, name: String, value: String, scope: String, scope_name: String): [Attribute]
}
