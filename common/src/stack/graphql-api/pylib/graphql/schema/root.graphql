type Query
type Mutation

interface Result {
	success: Boolean!
	error: String
}

type Status implements Result {
	success: Boolean!
	error: String
}

type Lookup {
	name: String!
	exists: Boolean!
}

enum Scope {
	global
	appliance
	os
	environment
	host
}
